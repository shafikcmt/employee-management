@extends('layouts.app')@section('title','Extra Duty')@section('content')<form class="card grid gap-3 md:grid-cols-2" method="post" action="{{ $item->exists?route('extra-duties.update',$item):route('extra-duties.store') }}">@csrf @if($item->exists)@method('put')@endif<select class="input" name="employee_id">@foreach($employees as $e)<option value="{{ $e->id }}" @selected($item->employee_id==$e->id)>{{ $e->name }}</option>@endforeach</select><select class="input" name="project_id"><option value="">Project</option>@foreach($projects as $p)<option value="{{ $p->id }}" @selected($item->project_id==$p->id)>{{ $p->name }}</option>@endforeach</select><input class="input" type="date" name="duty_date" value="{{ optional($item->duty_date)->format('Y-m-d') }}"><select class="input" name="day_type">@foreach(['friday','public_holiday','off_day','normal_day'] as $x)<option @selected($item->day_type==$x)>{{ $x }}</option>@endforeach</select><input class="input" name="hours_worked" placeholder="Hours" value="{{ $item->hours_worked }}"><input class="input" name="rate" placeholder="Rate" value="{{ $item->rate }}"><button class="btn-primary">Save</button></form>@endsection
