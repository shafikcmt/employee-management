@extends('layouts.app')@section('title','Payment')@section('content')<form class="card grid gap-3 md:grid-cols-2" method="post" action="{{ $item->exists?route($route.'.update',$item):route($route.'.store') }}">@csrf @if($item->exists)@method('put')@endif<select class="input" name="employee_id">@foreach($employees as $e)<option value="{{ $e->id }}" @selected($item->employee_id==$e->id)>{{ $e->name }}</option>@endforeach</select><select class="input" name="project_id"><option value="">Project</option>@foreach($projects as $p)<option value="{{ $p->id }}" @selected($item->project_id==$p->id)>{{ $p->name }}</option>@endforeach</select><input class="input" name="amount" value="{{ $item->amount }}" placeholder="Amount"><input class="input" type="date" name="payment_date" value="{{ optional($item->payment_date)->format('Y-m-d') }}"><input class="input" name="payment_type" value="{{ $item->payment_type ?: 'manual' }}"><button class="btn-primary">Save</button></form>@endsection
